// import React from 'react';
// import MarkdownRenderer from '../../components/MarkdownRenderer.tsx';
// // Import the raw Markdown content using Vite's ?raw suffix
// // Ensure the path is correct relative to this file.
// import documentationMarkdown from '../../src/assets/markdown/triples-documentation.md?raw';

// const TriplesDocumentationPage: React.FC = () => {
//   return (
//     <div className="bg-white p-6 sm:p-8 rounded-xl shadow-xl max-w-4xl mx-auto">
//       {/* 
//         The overall page title and introduction can be part of the Markdown content itself,
//         typically starting with an H1 (e.g., # My Document Title).
//         The <header> element previously here can be removed if its content
//         is now in the .md file.
//       */}
//       <MarkdownRenderer markdownContent={documentationMarkdown} />
//     </div>
//   );
// };

// export default TriplesDocumentationPage;